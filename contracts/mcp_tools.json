[
  {
    "name": "answer",
    "description": "Buy a synthesized answer from the owner's approved publications, in the owner's voice. Payment settles at submission and returns a ticket immediately; poll result until it completes. Ask can_answer first: no coverage means refusal after payment, and there are no automated refunds.",
    "required": [
      "question"
    ],
    "surfaces": [
      "worker"
    ]
  },
  {
    "name": "can_answer",
    "description": "Free coverage probe for the paid answer tool: reports whether the owner's publications can answer a question, with the price. Questions sent to this node are retained and visible to its owner.",
    "required": [
      "question"
    ],
    "surfaces": [
      "worker"
    ]
  },
  {
    "name": "discover",
    "description": "Return this node's full catalog of owner-approved publications: teasers grouped by topic, with ids, freshness, and price. Free. Choose zero, one, multiple, or all ids; call get once per chosen id.",
    "required": []
  },
  {
    "name": "get",
    "description": "Fetch one owner-approved publication by its id from the discover catalog. Each call buys exactly one publication. Damaged ids are rejected before payment; use a current catalog because a just-revoked id can still be billed.",
    "required": [
      "id"
    ]
  },
  {
    "name": "result",
    "description": "Fetch the outcome of a paid answer ticket. Free and idempotent; keep polling while status is running. Terminal statuses: complete, refused (no coverage), failed (agent error or timeout — no automated refund yet).",
    "required": [
      "ticket"
    ],
    "surfaces": [
      "worker"
    ]
  }
]
