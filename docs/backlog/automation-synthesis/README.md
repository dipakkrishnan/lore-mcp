# automation-synthesis

Prefix: `AUT`

Covers: `lore/automation.py` (profile.json, per-agent prompt generation), and
the native scheduled tasks that run agent-assisted memory synthesis
(`~/.lore/automation/`).

Backlog items about the synthesis profile, prompt generation, or scheduling
integration with Claude/Codex go here.
