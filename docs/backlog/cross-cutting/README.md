# cross-cutting

Prefix: `XC`

Covers: work that spans multiple components (e.g. a schema change touching
`store.py` and `mcp.py`), or project-wide concerns (release process, testing
strategy, packaging) that don't belong to one component folder.

Backlog items that don't fit cleanly into a single component go here.
