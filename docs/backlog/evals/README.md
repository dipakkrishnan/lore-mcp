# evals

Prefix: `EVAL`

Covers: the quality benchmark under `evals/` — the case suite, the runner,
judging, and its evolution toward exercising the real Lore pipeline instead
of prompt roleplay. The user-facing library evaluation skill (ONB-003 in
PR #33) is onboarding's; items here are about the harness both would share.
