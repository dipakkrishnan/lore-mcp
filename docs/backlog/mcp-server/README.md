# mcp-server

Prefix: `MCP`

Covers: `lore/mcp.py` — the stdio/Streamable HTTP MCP server, `discover`/`answer`
tools, and the loopback HTTP origin.

Backlog items about the MCP protocol surface, tool behavior, or transport go
here.
