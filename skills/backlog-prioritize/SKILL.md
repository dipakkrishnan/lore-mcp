---
name: backlog-prioritize
description: Re-rank ready and in-review backlog items under docs/backlog/ by impact vs. effort, respecting blockers. Use when the user says "prioritize the backlog", "re-rank the backlog", "what should we work on next", or "groom the backlog" (paired with an audit).
---

# Backlog: prioritization

Read `docs/backlog/agents/AGENTS.md` for the shared rules, then
`docs/backlog/agents/prioritization.md` and follow it exactly. This skill
never creates new items — if you notice something missing while ranking,
report it as a suggestion for `backlog-ideate` rather than adding it here.

Do not regenerate `docs/backlog/INDEX.md` yourself — end by suggesting the
`backlog-audit` skill (or running the `audit` playbook) to reflect the new
ranking.
